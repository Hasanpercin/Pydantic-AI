# .env.example (GitHub'a commit edilir)
# .env (local, .gitignore'da - GİZLİ)

# Bu dosyayı kopyalayıp .env olarak kaydedin ve gerçek değerlerle doldurun

# Core
ENVIRONMENT=production
PORT=8000
LOG_LEVEL=INFO

# Anthropic (REQUIRED)
# https://console.anthropic.com/settings/keys adresinden alın
ANTHROPIC_API_KEY=sk-ant-api03-REPLACE-WITH-YOUR-REAL-KEY
ANTHROPIC_MODEL=claude-sonnet-4-20250514
ANTHROPIC_MODEL_FAST=claude-haiku-4

# Redis (Level 5+)
# Dokploy'dan Internal Host, Password kopyalayın
# Format: redis://default:PASSWORD@INTERNAL-HOST:6379/0
REDIS_URL=redis://default:Astroredis@yeni-proje-astrocalcredis-ch0iti:6379/0

# Zep Memory (Level 3+)
# https://www.getzep.com adresinden API key alın
ZEP_API_KEY=z_REPLACE-WITH-YOUR-ZEP-KEY
ZEP_API_URL=https://api.getzep.com

# Calculation Engine (Level 2+)
# Kendi Calculation Engine URL'nizi girin
CALC_ENGINE_URL=https://calc.yourdomain.com
CALC_ENGINE_API_KEY=calc_REPLACE-WITH-YOUR-KEY

# Monitoring (optional)
# https://logfire.pydantic.dev adresinden API key alın
LOGFIRE_API_KEY=lf_REPLACE-WITH-YOUR-KEY

# CORS (Mobile app için)
CORS_ORIGINS=*

# Security
# Python ile generate edin: python -c "import secrets; print(secrets.token_hex(32))"
SECRET_KEY=REPLACE-WITH-RANDOM-32-BYTE-HEX
